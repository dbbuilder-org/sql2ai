<Window x:Class="SSMSAI.SSMS.UI.ConfigurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SSMS-AI Configuration" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Configure AI Provider Settings" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>
        
        <!-- API Provider Selection -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="AI Provider:" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="ProviderComboBox" 
                      Width="200" 
                      SelectedValue="{Binding ApiProvider}"
                      SelectionChanged="ProviderComboBox_SelectionChanged">
                <ComboBoxItem Content="OpenAI" IsSelected="True"/>
                <ComboBoxItem Content="Google Gemini" IsEnabled="False"/>
                <ComboBoxItem Content="Anthropic Claude" IsEnabled="False"/>
            </ComboBox>
        </StackPanel>
        
        <!-- API Key -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="API Key:" Width="120" VerticalAlignment="Center"/>
            <PasswordBox x:Name="ApiKeyPasswordBox" 
                         Width="200" 
                         PasswordChanged="ApiKeyPasswordBox_PasswordChanged"/>
        </StackPanel>
        
        <!-- Model Selection -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="Model:" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="ModelComboBox" 
                      Width="200"
                      SelectedValue="{Binding Model}">
                <ComboBoxItem Content="gpt-3.5-turbo"/>
                <ComboBoxItem Content="gpt-4"/>
                <ComboBoxItem Content="gpt-4-turbo"/>
            </ComboBox>
        </StackPanel>
        
        <!-- Temperature -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="Temperature:" Width="120" VerticalAlignment="Center"/>
            <Slider x:Name="TemperatureSlider" 
                    Width="150" 
                    Minimum="0" 
                    Maximum="1" 
                    Value="{Binding Temperature}"
                    TickFrequency="0.1" 
                    IsSnapToTickEnabled="True"
                    VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ElementName=TemperatureSlider, Path=Value, StringFormat=F1}" 
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0"/>
        </StackPanel>
        
        <!-- Max Tokens -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="Max Tokens:" Width="120" VerticalAlignment="Center"/>
            <TextBox x:Name="MaxTokensTextBox" 
                     Width="100" 
                     Text="{Binding MaxTokens}"
                     VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Test Connection Button -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,20,0,0">
            <Button x:Name="TestConnectionButton" 
                    Content="Test Connection" 
                    Width="120" 
                    Height="30"
                    Click="TestConnectionButton_Click"/>
            <TextBlock x:Name="TestResultTextBlock" 
                       VerticalAlignment="Center" 
                       Margin="15,0,0,0"/>
        </StackPanel>
        
        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="OK" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
